# ===== 部署模式配置 =====
# 部署模式：local（本地开发）或 cloud（云部署）
# 如果设置为 cloud，将使用下面的公网IP/域名配置
# 如果留空或设置为 local，将使用本地配置
DEPLOY_MODE=local

# ===== 公网IP/域名配置（云部署时使用） =====
# 如果 DEPLOY_MODE=cloud，将使用以下配置
# 公网IP示例：PUBLIC_IP=123.45.67.89
# 域名示例：PUBLIC_DOMAIN=your-domain.com
# 注意：如果设置了 PUBLIC_DOMAIN，将优先使用域名
PUBLIC_IP=
PUBLIC_DOMAIN=

# ===== 后端配置 =====
SECRET_KEY=dev-secret-key-change-in-production
DATABASE_URL=sqlite:///table_extractor.db
UPLOAD_FOLDER=uploads
MAX_FILE_SIZE=10485760
HOST=0.0.0.0
PORT=5000

# ===== 前端配置 =====
VITE_PORT=5173

# ===== CORS配置（可选，自动根据部署模式生成） =====
# 如果留空，将根据 DEPLOY_MODE、PUBLIC_IP、PUBLIC_DOMAIN 自动生成
# 也可以手动覆盖（多个地址用逗号分隔）
# 本地开发示例：CORS_ORIGINS=http://localhost:5173,http://localhost:3000
# 云部署示例：CORS_ORIGINS=http://your-domain.com,https://your-domain.com
CORS_ORIGINS=

